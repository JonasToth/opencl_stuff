# include all submodules


# exe contains the source code for all executables,
add_subdirectory(exe)
# lib contains source code, that is used in exectuables, but is not specific to them
add_subdirectory(lib)


